---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
---

<Layout title="Pago Exitoso | AMG SHOES">
    <Header />
    <main class="max-w-xl mx-auto px-6 py-20 text-center">
        <div class="mb-8 bg-black text-white w-24 h-24 rounded-full flex items-center justify-center mx-auto shadow-2xl animate-bounce">
            <svg class="w-12 h-12" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7"/></svg>
        </div>
        <h1 class="text-4xl font-black uppercase italic tracking-tighter mb-4 text-black">PAGO CONFIRMADO</h1>
        <p class="text-xs font-bold text-gray-400 uppercase tracking-[0.3em] mb-12">Tu pedido está siendo procesado en estos momentos</p>
        
        <div class="bg-gray-50 p-8 rounded-3xl border border-gray-100 text-left mb-10">
            <p class="text-[10px] font-black uppercase text-gray-400 mb-4">Información de Seguimiento</p>
            <p class="text-sm text-gray-700 leading-relaxed uppercase font-bold">Te mantendremos informado via Whatsapp o Correo, El tiempo para ver el QC es de 1 a 3 dias habiles y el envio 9 a 20 dias habiles (13.5 Dias promedio)</p>
        </div>

        <a href="/" class="inline-block bg-black text-white px-10 py-5 rounded-2xl font-black uppercase tracking-widest text-[10px]">Volver al Inicio</a>
    </main>
</Layout>